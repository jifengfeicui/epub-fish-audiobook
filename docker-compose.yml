services:
  alexandria:
    build: .
    ports:
      - "4200:4200"
    environment:
      ALEXANDRIA_DATA_DIR: /alexandria/data
    volumes:
      - ./data:/alexandria/data
    restart: unless-stopped
